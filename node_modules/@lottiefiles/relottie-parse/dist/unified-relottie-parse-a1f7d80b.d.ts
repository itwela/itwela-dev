import { Root } from '@lottiefiles/last';
import { Plugin } from 'unified';
import { ParseOptions } from './options.js';
import { Data, VFile } from 'vfile';

/**
 * Copyright 2022 Design Barn Inc.
 */

interface ParseFileData extends Data {
    parse: {
        messages?: VFile['messages'];
    };
}
interface Info {
    hasExpressions: Root['hasExpressions'];
}
declare function parse(document: string, file: VFile, settings?: SettingsOptions): Root;

/**
 * Copyright 2022 Design Barn Inc.
 */

declare type Options = Partial<ParseOptions>;
/**
 * Include other plugins' options
 */
interface SettingsOptions extends Record<string, unknown> {
    parse?: Options;
}
declare type FileData = Partial<ParseFileData>;
declare const relottieParse: Plugin<[Options?], string, Root>;

export { FileData as F, Info as I, Options as O, ParseFileData as P, SettingsOptions as S, parse as p, relottieParse as r };
