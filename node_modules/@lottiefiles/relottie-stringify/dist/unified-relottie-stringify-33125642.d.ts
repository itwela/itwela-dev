import { Root } from '@lottiefiles/last';
import { Plugin } from 'unified';
import { StringifyOptions } from './options.js';
import { Data, VFile } from 'vfile';

/**
 * Copyright 2023 Design Barn Inc.
 */

interface StringifyFileData extends Data {
    stringify?: object;
}
/**
 * Converts a Lottie AST back into the LottieJSON
 * @returns The JSON representation of the Lottie AST.
 */
declare function stringify(node: Root, _file: VFile, settings?: SettingsOptions): string;

/**
 * Copyright 2022 Design Barn Inc.
 */

declare type Options = Partial<StringifyOptions>;
/**
 * Include other plugins' options
 */
interface SettingsOptions extends Record<string, unknown> {
    stringify?: Options;
}
declare type FileData = Partial<StringifyFileData>;
declare const relottieStringify: Plugin<[Options?], Root, string>;

export { FileData as F, Options as O, StringifyFileData as S, SettingsOptions as a, relottieStringify as r, stringify as s };
